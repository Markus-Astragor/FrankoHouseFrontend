import React from "react";
import { BlockForLoader, Loader } from "./LoaderComponentStyles";

function LoaderComponent() {
  return (
    <BlockForLoader>
      <Loader></Loader>
    </BlockForLoader>
  );
}

export default LoaderComponent;
